/* globals Util: true, FilledInAnswer: true */

var FilledInAnswers = Backbone.Collection.extend({
	// model: FilledInAnswer,
	// url: Util.api + "filledinanswers",

	// save: function() {
	// 	this.each(function(filledInAnswer, index) {
	// 		filledInAnswer.save();
	// 	}, this);
	// }
});